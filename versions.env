# Arpwatch Docker Image Version Configuration
# This is the single source of truth for version information
# Used by: Dockerfile, Makefile, build.sh, and test scripts

# Arpwatch version (installed via apt, use Debian version)
# Check available versions: apt-cache madison arpwatch
VERSION=2.1a15

# Debian base image version
DEBIAN_VERSION=trixie

# Auto-populated during build (do not modify manually)
BUILD_DATE=
VCS_REF=
